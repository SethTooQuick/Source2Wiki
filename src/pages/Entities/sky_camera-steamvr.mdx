---
hide_table_of_contents: true
---

<!---
!!!!!!
THIS PAGE IS AUTOGENERATED FROM GAME FGD DEFINITIONS!
DO NOT EDIT MANUALLY!
!!!!!!

In order to make edits, you can make an annotation file in /fgd_dump_overrides
-->


Point Entity

An entity used to control the 3D Skybox. Its origin is used to determine the 3D Skybox's position relative to the map. Place this entity, in the 3D Skybox, at the point where the origin of the map should be.

<details open>
<summary><h2>Keyvalues</h2></summary>
- **3D Skybox scale** (`scale`) \<`Integer`\>\
Scale of the skybox.

- **Fog Enable** (`fogenable`) \<`Boolean`\>

- **Fog Blend** (`fogblend`) \<`Boolean`\>

- **Use Angles for Fog Dir** (`use_angles`) \<`Boolean`\>

- **Clip 3D sky near plane to world far plane** (`clip_3D_skybox_near_to_world_far`) \<`Boolean`\>

- **3D sky near plane offset** (`clip_3D_skybox_near_to_world_far_offset`) \<`String`\>\
Offset for the near clip plane for the 3D sky in world units.  This is only used if the above field is true.  Negative values to pull closer, etc.

- **Primary Fog Color** (`fogcolor`) \<`Color255`\>

- **Secondary Fog Color** (`fogcolor2`) \<`Color255`\>

- **Primary Fog Direction** (`fogdir`) \<`String`\>

- **Fog Start** (`fogstart`) \<`String`\>\
Distance at which the skybox fog should start.

- **Fog End** (`fogend`) \<`String`\>\
Distance at which the skybox fog should be fully opaque.

- **Fog Max Density [0..1]** (`fogmaxdensity`) \<`Float`\>

- **HDR color scale.** (`HDRColorScale`) \<`Float`\>\
float value to multiply fog color by when running in HDR mode.

- **SkyboxSlot** (`SkyboxSlot`) \<`String`\>\
Name of the skybox this camera belongs to.


 </details>

