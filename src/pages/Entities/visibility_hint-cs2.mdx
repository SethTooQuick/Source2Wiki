---
hide_table_of_contents: true
---

import visibility_hintIcon from '@site/static/img/Entities/cs2/visibility_hint.png';

<img src={visibility_hintIcon} alt="visibility_hint icon" style={{height: '80px'}} />

Point Entity



<details open>
<summary><h2>Keyvalues</h2></summary>
- **Box Mins** (`box_mins`) \<`Vector`\>

- **Box Maxs** (`box_maxs`) \<`Vector`\>

- **Hint Type** (`hintType`) \<`Choices`\>
  - Use High Resolution (8 unit grid) 
  - Use Normal Resolution (8 unit grid) 
  - Use Reduced Resolution (16 unit grid) 
  - Use Medium Resolution (32 unit grid) 
  - Use Low Resolution (64 unit grid) 
  - Use Lower Resolution (64 unit grid, fewer intial clusters) 
  - Use Lowest Resolution (256 unit grid, fewest initial clusters) 
  - X-axis split 
  - Y-axis split 
  - Z-axis split 



 </details>

